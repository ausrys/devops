version: 3
projects:
  - dir: .
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
    workflow: default
    terraform_version: v1.6.6
    workspace: default
    terraform_args: ["-var-file=secrets.auto.tfvars"]
